import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Base styles and global variables
import App from './App';

// Render the main App component
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
